apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: {{ .Chart.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    heritage: {{ .Release.Service }}
  name: {{ .Chart.Name }}
  namespace: default
  selfLink: /apis/extensions/v1beta1/namespaces/default/deployments/{{ .Chart.Name }}
spec:
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  strategy:
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}
      name: {{ .Chart.Name }}
    spec:
      nodeSelector:
        cloud.google.com/gke-nodepool: {{ .Values.service.nodePool }}
      containers:
      - name: {{ .Chart.Name }}
        image: "{{ .Values.image.repo }}:{{ .Values.image.tag }}"
        imagePullPolicy: Always
        env:
          - name: BASE_STORAGE_URL
            valueFrom:
              configMapKeyRef:
                key: baseStorageUrl
                name: {{ .Chart.Name }}
          - name: BASE_STREAM_URL
            valueFrom:
              configMapKeyRef:
                key: baseStreamUrl
                name: {{ .Chart.Name }}
          - name: SMCA
            valueFrom:
              configMapKeyRef:
                key: smca
                name: {{ .Chart.Name }}
          - name: VERIFIER_HTTP_ADDR
            valueFrom:
              configMapKeyRef:
                key: verifierHttpAddr
                name: {{ .Chart.Name }}
          - name: MANAGER_HTTP_ADDR
            valueFrom:
              configMapKeyRef:
                key: managerHttpAddr
                name: {{ .Chart.Name }}
          - name: BUCKET
            valueFrom:
              configMapKeyRef:
                key: bucket
                name: {{ .Chart.Name }}
=          - name: BLOCKCHAIN_URL
            valueFrom:
              secretKeyRef:
                key: blockchainUrl
                name: {{ .Chart.Name }}
          - name: NATS_URL
            valueFrom:
              secretKeyRef:
                key: natsUrl
                name: {{ .Chart.Name }}
          - name: PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Chart.Name }}
                key: password
          - name: HASH_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Chart.Name }}
                key: hash
          - name: KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Chart.Name }}
                key: key
          - name: OUTPUT_DIR
            value: /opt/mnt/
        ports:
          - containerPort: 80
            protocol: TCP
        resources:
          requests: {}
